---
title: "Create Rules"
description: "Create Rules with this endpoint. Rules are instructions on how to reward. Users are rewarded based on rules set."
---

import { NoteStatement, Table } from "/snippets/custom-component.mdx";

<NoteStatement
  label="PS"
  message="You can also create rules on your dashbaord. Rule Name must be in lower case and must be unique."
/>

####

import { URLBlock } from "/snippets/url-block.mdx";

<URLBlock
  url="https://test-api.bvndle.com/api/v1/laas/rules/create-rule"
  content={[
    { title: "HTTP Method", caption: "POST" },
    {
      title: "Authorization Header",
      caption: "Bearer ACCESS_TOKEN",
    },
  ]}
/>

####

<Table
  data={[
    { name: "name", type: "string", desc: "Required Rule Name" },
    {
      name: "coinValue",
      type: "string",
      desc: "Required Amount of coin to be given",
    },
    {
      name: "maxNumberOfParticipant",
      type: "string",
      desc: "Required Maximum Number of Participants (Number of users who can be rewarded.)",
    },
    {
      name: "condition",
      type: "string",
      desc: "Required (if type is not combined) More instruction or condition to be fulfilled",
    },
    {
      name: "status",
      type: "string",
      desc: "Optional \n Default: active Values: “active” or “inactive”",
    },
    {
      name: "type",
      type: "string",
      desc: "Optional Default: single Values: single, batched, combined",
    },
    {
      name: "completionNumber",
      type: "string",
      desc: "Optional (if type is not batched) Default: 1",
    },
    {
      name: "userLimitAward",
      type: "string",
      desc: "Optional Pass “-1” if you do not want a maximum number Default: -1",
    },
    {
      name: "rules",
      type: "string",
      desc: "Optional (if type is not combined) Array of object containing the rules data {name: string, condition: string, status: string}",
    },
  ]}
/>

```bash cURL
curl --request GET 'https://test-api.bvndle.com/api/v1/laas/rules/create-rule' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer ACCESS_TOKEN' \
--data-raw '{
  "name": "Transaction Drive",
  "coinValue": "10",
  "maxNumberOfParticipant": "20000",
  "condition": "User makes 5 transactions"
}
{
  "userId": "f0c8c81d-13d3-4515-b40f-711ef91de094",
  "ruleName": "transaction drive"
}'
```

<ResponseExample>

```json Success
{
  "status": "success",
  "message": "rule created successfully.",
  "data": {
    "id": "bddd3fec-edbd-4fd9-8519-07d4a309d395",
    "name": "documenting",
    "coinValue": "10",
    "condition": "user writes 5 lines",
    "maxNumberOfParticipant": "20000",
    "status": "active"
  }
}
```

```json Error 400
{
  "status": "failed",
  "message": "invalid data provided",
  "data": {
    "errorType": "invalid",
    "appErrorCode": "Bad request",
    "error": "\"name\" is required"
  }
}
```

</ResponseExample>
